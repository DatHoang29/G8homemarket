const CONFIG = {
  STORAGE_KEYS: {
    USER_INFO: "userInfo",
    DELIVERY: "delivery",
    SHIPPING_ADDRESS: "shippingAddress",
  },
  // Vendure / shop API used for server-side authentication using Zalo phone token
  VENDURE_API: "https://chae-unbeguiled-maudie.ngrok-free.dev/shop-api",
};

export default CONFIG;
